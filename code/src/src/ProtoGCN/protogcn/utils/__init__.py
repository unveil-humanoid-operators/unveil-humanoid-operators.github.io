from .graph import Graph  # noqa: F401


def cache_checkpoint(filename, cache_dir='.cache'):
    """URL-checkpoint caching was trimmed from this bundle — local checkpoint
    paths are returned unchanged."""
    return filename
